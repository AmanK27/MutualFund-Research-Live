# v1.5.0

## [1.5.0] - 2026-03-02

### Added
- **Live Search & Autocomplete:** Natural language fund search via `mfapi.in` with debounced input and client-side RAM caching.
- **Live AMFI Table Parsing:** Replaced `MOCK_FUNDS` with a client-side text parser that directly downloads and maps `NAVAll.txt` from AMFI into categorized lists natively on the browser.
- **Custom Workflow Rules:** Configured hardcoded remote rule for GitHub deployments enforcing a localized `changelog/` dir.
